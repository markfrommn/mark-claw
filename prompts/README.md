# prompts/

Per-pipeline `claude -p` prompts (design §6.3), e.g. `briefing.md`,
`triage-judge.md`, `eod.md`. Added by the pipeline issues. The agentic layer
reads spool files and prompts and never sees secrets.
