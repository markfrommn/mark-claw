# bin/

Orchestrators, provider wrappers, action tools, and the `mclaw` CLI shim live
here (design §6.3). The CLI itself is installed as the `mclaw` console script
pointing at `mclaw_core.cli:main`; per-pipeline wrapper scripts land in later
issues. No personal data, no secrets.
